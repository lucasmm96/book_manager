doctype html
html(lang="en")
  include ../includes/head.pug
  body
    include ../includes/navbar.pug
    main
      if bookList.length > 0
        table
          tr
            th Title
            th Options
          each book in bookList
            tr
              td= book.title
              td
                a(href=('/user/manage-book/edit/' + book._id)) Edit
                a(href=('/user/manage-book/detail/' + book._id)) Details
                a(href=('/user/manage-book/delete/' + book._id)) Delete
      else
        h2 Your list is empty.
    include ../includes/footer.pug
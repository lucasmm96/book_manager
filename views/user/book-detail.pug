doctype html
html(lang="en")
  include ../includes/head.pug
  body
    include ../includes/navbar.pug
    main
      h1 Book detail
      .book-card
        h2=book.title
        p=(book.author === undefined || book.author === null ? 'Author: Not set' : 'Author: ' + book.author)
        p=(book.addedAt === undefined || book.addedAt === null ? 'AddedAt: Not set' : 'AddedAt: ' + book.addedAt)
        p=(book.finishedAt === undefined || book.finishedAt === null ? 'FinishedAt: Not set' : 'FinishedAt: ' + book.finishedAt)
        p=(book.score === undefined || book.score === null ? 'Score: Not set' : 'Score: ' + book.score)
        p=(book.status === undefined || book.status === null ? 'Status: Not set' : 'Status: ' + book.status)
        a(href=('/book/' + book._id))
    include ../includes/footer.pug